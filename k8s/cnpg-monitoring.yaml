apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: cnpg-pg-asanti
  namespace: monitoring
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: pg-asanti
  podMetricsEndpoints:
    - port: metrics
      interval: 10s
